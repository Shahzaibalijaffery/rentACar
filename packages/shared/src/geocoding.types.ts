export type AreaSearchResult = {
  /** Full label shown in search results. */
  label: string;
  /** Short area label stored on the vehicle and shown in discovery. */
  areaLabel: string;
  latitude: number;
  longitude: number;
};
